<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/HomeFragment">

    <fragment
        android:id="@+id/HomeFragment"
        android:name="uz.suxa.metaworship.presentation.HomeFragment"
        android:label="@string/app_name"
        tools:layout="@layout/fragment_home">

        <action
            android:id="@+id/action_HomeFragment_to_NewSongFragment"
            app:destination="@id/NewSongFragment" />
    </fragment>
    <fragment
        android:id="@+id/NewSongFragment"
        android:name="uz.suxa.metaworship.presentation.NewSongFragment"
        android:label="@string/new_song_fragment_label"
        tools:layout="@layout/fragment_new_song">

        <action
            android:id="@+id/action_NewSongFragment_to_HomeFragment"
            app:destination="@id/HomeFragment" />
    </fragment>
</navigation>